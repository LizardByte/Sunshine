---
description: Unauthorized - The request requires user authentication.
content:
  application/json:
    schema:
      type: object
      properties:
        status_code:
          type: integer
        status:
          type: boolean
        error:
          type: string
    example:
      status_code: 401
      status: false
      error: "Unauthorized"