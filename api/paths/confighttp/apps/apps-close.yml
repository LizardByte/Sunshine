---
post:
  summary: Close the running application.
  description: Close the currently running application.
  operationId: closeApp
  tags:
    - Apps
  responses:
    '200':
      description: The application was closed successfully.
      content:
        application/json:
          schema:
            type: object
            properties:
              status:
                type: boolean
          example:
            status: true
    '401':
      $ref: "../../../components/responses/401.yml"
    '403':
      $ref: "../../../components/responses/403.yml"